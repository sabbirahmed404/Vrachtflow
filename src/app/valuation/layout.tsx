// Stepper layout for valuation tool
export default function ValuationLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="valuation-layout">
      {/* Stepper navigation can be added here */}
      {children}
    </div>
  );
}
